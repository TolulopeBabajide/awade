# Code Review — 2026-07-05 · 686ffdb

**Verdict**: ✅ Clean
**Files reviewed**: 2 (api.ts, api.test.ts; backlog.md and agent-health file skipped as non-code)
**Commits covered**: 686ffdb (fix/polish/AWD-L-85-dev-guard-console-error)
**Issue**: AWD-L-85 — `console.error("Refresh failed", e)` at `api.ts:75` not wrapped in DEV guard

## Summary Table

| Category | Findings | Worst Severity |
|----------|----------|----------------|
| SOLID | 0 | — |
| Complexity | 0 | — |
| Duplication | 0 | — |
| Coupling | 0 | — |
| Naming | 0 | — |
| Error Handling | 0 | — |
| Security (structural) | 0 | — |
| API Design | 0 | — |
| Test quality | 2 | 🟢 |

## Findings

### apps/frontend/src/services/api.ts
- No issues. The single-line change at L75 (`if (import.meta.env.DEV) console.error(...)`) is correct and matches the established codebase pattern exactly (websocket.ts:51,62,67,73,78 and all admin/*.tsx files use the same inline guard form). Grep confirmed this is the only `console.*` call in the file.

### apps/frontend/src/test/services/api.test.ts
- **🟢 Low** Line 126: `vi.spyOn(apiService as any, 'refreshAccessToken')` — spy via `as any` on a private method is rename-brittle. Acceptable for an XS fix; no alternative without making the method internal-testable. Note for future: if `refreshAccessToken` is renamed, this test will silently degrade.
- **🟢 Low** Line 137: Only the DEV=`true` path is asserted. The `!DEV` path (console.error not called in production builds) is not tested. Testing it would require mocking `import.meta.env.DEV`, which is non-trivial in vitest. Given the trivial one-line guard, the omission is acceptable.

## Backlog Items Filed
None — both observations are pre-existing conditions or too minor for backlog items.

## Notes
- The `catch (e)` block at `api.ts:L74-76` is near-dead code in the current implementation: `refreshAccessToken()` has its own internal try-catch that returns `false` on any error, so the outer catch in `handleResponse` can only be triggered if `refreshAccessToken` itself throws (which is only testable via spy). The fix is still correct as defense-in-depth. This is a pre-existing structural note, not introduced by this diff. It is related to the broader `M-120` refactor backlog item.
- `api.ts` is 872 lines — already tracked in `M-120` (split by domain). Not re-filed.
- This is a clean, minimal fix. Zero scope creep.

📝 **Feedback prompt**: If you revise this output significantly before using it, please log it —
`"Log feedback: code-review-agent output was [approved / revised / rejected] — [what changed]"`
Logs go to `docs/agentic/feedback-log.md` and improve future prompts.
