# Financial Snapshot — 2026-04-24

> Automated run by `awade-friday-finance` scheduled task. Agent: finance-agent.
> Revenue stage per `project-config.md` §4: **pre-revenue**.
> Prior snapshot: [2026-04-21](snapshot-2026-04-21.md) — this is week 2.

---

## Revenue

- **MRR:** $0 (↔ no change vs 2026-04-21 — still pre-revenue)
- **ARR:** $0
- **New subscribers this week:** 0 monthly + 0 annual
- **Churned:** 0
- **Payment provider:** Stripe planned — not yet integrated (`project-config.md` §4 `PAYMENT_PROVIDER`)

No billing system exists to pull from. Stripe MCP listed as **not connected** in `project-config.md` §9b.

---

## Burn & Runway

### Cash balance
**Unknown — `docs/agentic/finance/balance.md` still does not exist.**
This was flagged as a 🔴 immediate alert in the 2026-04-21 snapshot. As of today (day 3 since first flag), the file has not been created. The 14-day stale threshold has not yet been breached, but the runway calculation remains blocked. See alerts below.

### Monthly burn — estimated
No invoiced figures are available. Cost notes under `docs/agentic/finance/` contain only this snapshot and the prior one. Estimates below are unchanged from the baseline snapshot and should be replaced with actuals.

| Cost driver | Est. monthly | Basis |
|---|---|---|
| Render — backend web service + Postgres | ~$14–25 | Render Starter web ($7) + Starter Postgres ($7); Pro ~$25+ |
| Vercel — frontend hosting | $0–20 | Hobby tier sufficient pre-launch; Pro ($20/mo) if team seats needed |
| OpenAI API — parent guides + lesson plans | ~$20–60 | Pre-launch dev/testing traffic; see note below re: H-07 |
| Domain + misc (DNS, email relay) | ~$2–5 | Amortised annual domain |
| **Estimated total burn** | **~$40–110 / mo** | Agent estimate — not from invoices |

**OpenAI cost note (improvement vs last week):** AWD-H-07 (missing rate limiter on `POST /children/{child_id}/guides/generate`) is now **resolved** — the endpoint has `@limiter.limit("5/minute")` per IP, confirmed in `security-report-2026-04-24.md` §LLM-4. The unbounded-spend risk flagged last week has been closed. This reduces the top end of the OpenAI burn estimate materially.

### Runway
**Cannot calculate — requires `docs/agentic/finance/balance.md`.** See alert 🔴-1 below.

### Break-even
Pre-revenue. Requires (a) Stripe integration shipped and (b) pricing set. Neither has a filed backlog ticket as of today.

---

## Unit Economics

Not meaningful pre-revenue. No paying customers, no acquisition spend.

- **LTV:** N/A
- **CAC:** $0 — no paid acquisition; all social channels "not set up" (`project-config.md` §8)
- **LTV:CAC ratio:** N/A
- **Payback period:** N/A

---

## Progress Toward First Paying Customer

**Target: Parent pivot public launch — June 2026** (`project-config.md` §1 `LAUNCH_TARGET`). ~5–6 weeks remain.

### Gating items blocking first dollar of revenue

| Item | Status this week | Change vs 2026-04-21 |
|---|---|---|
| Stripe integration | ❌ Not started; no backlog ticket | ↔ No change |
| Pricing decision (Pro tier, free-tier limits) | ❌ TBD per project-config §4 | ↔ No change |
| Signup → paid conversion plumbing (plan selector, trial, paywall) | ❌ Not built | ↔ No change |
| Analytics (PostHog / Amplitude) | ❌ Not connected (§5) | ↔ No change |
| Error monitoring (Sentry) | ✅ **Resolved** — AWD-H-01 shipped (commit `364762f`, merged `85c42e6`) | 🟢 Improved |

### Product foundation shipped this week (non-revenue but de-risks launch)
The dev loop resolved a large cluster of issues between 2026-04-21 and 2026-04-24. Key items with launch-readiness relevance:

- **AWD-H-01 Sentry** — backend + frontend error monitoring now wired. Running paid traffic without error visibility was a risk; this is now closed.
- **AWD-H-25** — Auth tokens migrated from localStorage to HttpOnly cookies (security hardening for launch).
- **AWD-H-19/H-20** — Dedicated `/children` page and parent onboarding flow now live. These are core parent UX features needed for launch.
- **AWD-H-07** — Rate limiter on guide generation endpoint (OpenAI cost protection resolved).
- **AWD-M-02 + M-37** — SEO meta tags and OG image (PNG) added to landing page. Social sharing now functional.
- **AWD-H-34** — `get_optional_current_user` updated for HttpOnly cookie auth; browser clients no longer treated as anonymous.

Remaining open items that could gate June launch (from backlog, finance-relevant subset):
- **AWD-H-36** (staged working tree regression — git staging issue, needs Tolu) — blocks clean CI run
- **AWD-C-05** (git refs/heads/develop corruption — requires Tolu to run recovery commands locally) — blocks CI pushes
- **GRC-01** (COPPA parental consent flow) — legal/compliance blocker for launch in US-adjacent markets
- **M-04** (backend coverage <70% in some modules) — CI enforced threshold; could block green CI

---

## Alerts

### 🔴 Immediate — action required from Tolu

**Alert 1 — `balance.md` still missing (day 3 of flag)**
`docs/agentic/finance/balance.md` was requested in the 2026-04-21 snapshot. It does not exist today. The 14-day stale threshold is not yet breached, but runway calculation has been blocked for a full week. Until this file exists, the finance agent cannot compute runway, cannot flag a <6-month threshold, and cannot assess whether burn is sustainable.

**Recommended minimum:** create the file and enter current cash-in-bank figure. Template repeated from last snapshot:
```markdown
# Awade Cash Balance
Last updated: 2026-04-24
## Current cash in bank
- Balance: $______
- Currency: USD (or note otherwise)
## Expected near-term movements
- Next top-up / funding: [date, amount, source]
- Known upcoming outflows: [e.g., annual domain renewal, any prepayments]
## Notes
- [Context — e.g., "Bootstrapped from personal savings; no runway pressure until Oct 2026"]
```

**Alert 2 — Many commits pending `git push origin develop` (CI unverified)**
The dev-log shows ~20+ commits since 2026-04-22 marked `CI:pending` because sandbox has no GitHub credentials. This means the CI pipeline has not validated any of this week's work in production. This is not a direct cash issue, but it means the codebase's quality gate is unverified. If CI is red after a push (especially AWD-C-05 or AWD-H-36), it could delay launch readiness and extend runway spend. **Tolu needs to `git push origin develop` and confirm CI green.**

**Alert 3 — AWD-C-05: git refs/heads/develop corrupted**
The develop branch ref points to a missing commit object. All git operations on develop fail. This blocks CI pushes entirely and must be resolved before any code ships to production. Requires Tolu to run the recovery commands locally (see backlog AWD-C-05 for exact steps).

### 🟡 Warning

**Alert 4 — Stripe not started; 5–6 weeks to June launch target**
No backlog ticket exists for Stripe integration as of today. Even a minimal Stripe checkout implementation typically takes 1–2 sprints. If Stripe is not filed and started in the next 1–2 weeks, the June 2026 launch target will slip on revenue capability. Consider filing this as `AWD-H-##` immediately.

**Alert 5 — No OpenAI cost trend data**
Cannot assess whether OpenAI API costs are trending up because no invoiced figures have been recorded. Tolu should paste the April billing total from the OpenAI dashboard into `docs/agentic/finance/openai-costs.md` at end of month. Without a baseline, the trend-alert threshold ("steeply up") cannot be computed.

**Alert 6 — COPPA consent flow (GRC-01) still open**
If Awade collects child profiles from US-resident parents at launch without a COPPA parental consent record, this creates legal/compliance exposure. This is a cost risk (potential fines, legal fees) in addition to a compliance one. Not urgent for non-US markets, but worth resolving before any paid marketing to US parents.

### 🟢 Positive developments vs last snapshot

- **H-07 resolved** — OpenAI rate limit on guide generation closed the biggest cost-risk exposure from last week. Estimated max OpenAI burn is now bounded per-IP.
- **Sentry wired (H-01)** — Error monitoring in place before launch; this reduces the risk of silent production failures that could damage retention and LTV for the first paying cohort.
- **Strong dev velocity this week** — 25+ issues resolved in 3 days. Foundation for June launch is meaningfully stronger.

---

## Recommended Action (one, for next week)

**File and begin Stripe integration as a High backlog issue.** With 5–6 weeks to June launch, Stripe is now the single highest-leverage unstarted item for revenue capability. Suggested format:

```
**AWD-H-37 — Stripe integration: checkout, subscription management, and webhook handler**
**Problem**: Awade has no payment infrastructure. Cannot charge a paying customer.
**Acceptance criteria**:
- [ ] Stripe Checkout or Payment Element wired on a /upgrade page
- [ ] Webhook handler for subscription.created, subscription.deleted, invoice.paid
- [ ] User.plan_tier field (free | pro) updated on subscription events
- [ ] Pro-tier paywall on AI generation endpoints (check plan_tier before hitting OpenAI)
- [ ] .env.example updated with STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, VITE_STRIPE_PUBLIC_KEY
**Files**: apps/backend/routers/ (new stripe.py), apps/backend/services/ (stripe_service.py), apps/frontend/src/pages/ (UpgradePage.tsx), apps/backend/models.py (User.plan_tier)
**Effort**: L (2+ days)
```

Secondary: create `docs/agentic/finance/balance.md` so runway can be calculated in next week's snapshot.

---

## Sources

- [project-config.md](../../../project-config.md) — §4 revenue stage/payment provider, §1 launch target, §5 analytics/monitoring, §8 social channels, §9b Stripe MCP.
- [finance-agent SKILL.md](../../../.claude/skills/finance-agent/SKILL.md) — snapshot format + alert thresholds.
- [snapshot-2026-04-21.md](snapshot-2026-04-21.md) — prior week baseline for comparisons.
- [backlog.md](../backlog.md) — AWD-C-05 (git corruption), AWD-H-36 (staging regression), GRC-01 (COPPA), M-04 (coverage).
- [security-report-2026-04-24.md](../audits/security-report-2026-04-24.md) — LLM-4 (H-07 rate limit confirmed resolved), Web-9 (Sentry confirmed wired).
- [sprints/dev-log.md](../sprints/dev-log.md) — confirmed issues resolved 2026-04-21 through 2026-04-24; CI:pending status on all pushes.
- `docs/agentic/finance/` — no balance.md, no openai-costs.md; only prior snapshot exists.

---

*Generated by Finance Agent · awade-friday-finance scheduled task · 2026-04-24*
