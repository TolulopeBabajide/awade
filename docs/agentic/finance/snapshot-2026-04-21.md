# Financial Snapshot — 2026-04-21

> Automated run by `friday-finance` scheduled task. Agent: finance-agent.
> Source of truth references in **Sources** footer. Revenue stage per `project-config.md` §4: **pre-revenue**.

This is the **first snapshot** written to `docs/agentic/finance/` — no prior weeks to compare against. Establishing the baseline.

---

## Revenue

- **MRR:** $0 (↔ no change — still pre-revenue)
- **ARR:** $0
- **New subscribers this week:** 0 monthly + 0 annual
- **Churned:** 0
- **Payment provider:** not yet integrated (Stripe planned — see `project-config.md` §4 `PAYMENT_PROVIDER`)

Stripe MCP is listed as **not connected** in `project-config.md` §9b. No billing system exists to pull from.

---

## Burn & Runway

### Cash balance
**Unknown — `docs/agentic/finance/balance.md` does not exist.** The finance directory was empty before this run (this snapshot is the first file in it). Tolu has not yet seeded a balance. **Runway cannot be calculated.**

### Monthly burn — estimated
No prior invoices or cost notes are tracked under `docs/agentic/finance/`. The estimates below are agent-generated defaults based on the stack described in `project-config.md` §2 and deployment setup in §13 — they should be replaced with actual invoiced numbers as soon as Tolu adds them.

| Cost driver | Est. monthly | Basis |
|---|---|---|
| Render — backend web service + Postgres | ~$14–25 | Render Starter web ($7) + Starter Postgres ($7); Pro tier pushes to ~$25+ if enabled |
| Vercel — frontend hosting | $0–20 | Hobby tier likely sufficient pre-launch; Pro ($20/mo) only if team seats or commercial use |
| OpenAI API — parent guides + lesson plans | ~$20–60 | Pre-launch dev/testing traffic only; unmetered at the app layer (see H-07 alert below) |
| Domain + misc (DNS, email relay) | ~$2–5 | Amortised annual domain |
| **Estimated total burn** | **~$40–110 / mo** | — |

Tolu's time is the largest real cost but is not cash burn and is excluded here per standard SaaS founder accounting.

### Runway
**Cannot calculate — requires `docs/agentic/finance/balance.md`.** See alert below.

### Break-even
Pre-revenue; break-even requires (a) Stripe integration shipped and (b) first paid tier pricing set. Neither has a backlog item yet per `docs/agentic/backlog.md`.

---

## Unit Economics

Not meaningful at this stage — no paying customers, no acquisition spend.

- **LTV:** N/A — no paid plan yet
- **CAC:** $0 — no paid acquisition (no ad spend; social channels all "not set up" per `project-config.md` §8)
- **Ratio:** N/A
- **Payback period:** N/A

Will become measurable once Stripe ships and the first paying cohort exists.

---

## Progress Toward First Paying Customer

Target: **Parent pivot public launch — June 2026** (per `project-config.md` §1 `LAUNCH_TARGET`).

Gating items blocking first dollar of revenue:

1. **Stripe integration** — not started; no backlog ticket located. Needs to be filed before June 2026 launch.
2. **Pricing decision** — "Pro tier (planned, TBD)" per `project-config.md` §4. Free-tier limits (AI generations/month) also TBD.
3. **Signup → paid conversion plumbing** — role picker exists (EDUCATOR/PARENT) but no plan selector, no trial logic, no paywall in `apps/frontend/src/pages/`.
4. **Analytics** — `ANALYTICS_TOOL: not yet connected` (§5). Cannot measure conversion without PostHog/Amplitude wired up.
5. **Error monitoring** — H-01 Sentry not connected; running paid traffic without error monitoring is risky.

Recommended sequencing: **H-01 (Sentry) → analytics → Stripe → pricing → paywall**. Suggest filing these as an `M-##` epic in the backlog if not already present.

---

## Alerts

### 🔴 Immediate — action required from Tolu

1. **`balance.md` does not exist.** Per the scheduled-task brief (threshold: >14 days stale), a missing file is worse than stale — runway can't be calculated at all. Tolu needs to create `docs/agentic/finance/balance.md` with current cash-in-bank and expected date of next top-up. A stub template is proposed below (see "Recommended Action").

2. **H-07 is still open and materially increases OpenAI cost risk.** `apps/backend/routers/children.py` line 129 (`POST /children/{child_id}/guides/generate`) has no `@limiter.limit` decorator, while the comparable lesson-plan endpoints do (`apps/backend/routers/lesson_plans.py:40`, `:154`). A single authenticated parent account can drive unbounded OpenAI spend. Source: `docs/agentic/audits/security-report-2026-04-21.md` §H-07 and `docs/agentic/backlog.md` line 37. **This is both a security issue and a finance issue — flagging from the finance side because it directly threatens burn if exploited or accidentally mis-used.**

3. **H-09 (OpenAI client no explicit timeout, `packages/ai/providers/openai_provider.py:27`)** — secondary cost risk: a hung worker under network degradation blocks throughput and can multiply retries/token spend. Source: same backlog / security report.

### 🟡 Warning

4. **No OpenAI cost trend data tracked.** Cannot assess "trend steeply up" per the scheduled-task brief because no prior data exists. Tolu should paste the monthly OpenAI dashboard total into `docs/agentic/finance/openai-costs.md` (proposed) after each billing cycle.

5. **No cost-observability hooks.** None of Render, Vercel, or OpenAI billing is connected via MCP. All cost numbers in this snapshot are agent-estimated, not invoiced. Accuracy will be low until Tolu seeds actuals.

### 🟢 Not an issue this week

- Pre-revenue + very small burn → runway risk is almost certainly low in absolute terms, even though it can't be numerically computed until balance.md exists.

---

## Recommended Action (one, for next week)

**Create `docs/agentic/finance/balance.md` with this week's cash-in-bank number.** Suggested template — Tolu just needs to fill in the numbers:

```markdown
# Awade Cash Balance

Last updated: 2026-04-__

## Current cash in bank
- Account: [bank / business account name]
- Balance: $______
- Currency: USD (or note otherwise)

## Expected near-term movements
- Next top-up / funding: [date, amount, source]
- Known upcoming outflows: [e.g., annual domain renewal, any prepayments]

## Notes
- [Any context — e.g., "Bootstrapped from personal savings; no runway pressure until Oct 2026"]
```

Once this file exists, future Friday snapshots can compute runway = balance / burn and flag the <6 month threshold automatically.

Secondary (if time): file **H-07 fix** (add `@limiter.limit("5/minute")` to `POST /children/{child_id}/guides/generate` to match the lesson-plan endpoints) — low effort, high leverage on cost-risk.

---

## Sources

- [project-config.md](../../../project-config.md) — §4 revenue stage, §9b Stripe MCP status, §1 launch target, §2 stack, §5 analytics/error-monitoring gaps, §8 social channels.
- [finance-agent SKILL.md](../../../.claude/skills/finance-agent/SKILL.md) — snapshot format + thresholds.
- [SCHEDULED-TASKS.md](../SCHEDULED-TASKS.md) — Task 6 `friday-finance` brief.
- [backlog.md](../backlog.md) — H-07 (line 37), H-09 (line 39).
- [security-report-2026-04-21.md](../audits/security-report-2026-04-21.md) — §H-07 parent guide rate limit, §H-09 OpenAI client timeout.
- `docs/agentic/finance/` — empty directory prior to this run; no historical cost notes available.
